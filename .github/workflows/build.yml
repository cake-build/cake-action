name: Build
on:
  pull_request:
  push:
    branches:
      - master
jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - name: Get the sources
        uses: actions/checkout@v1
      - name: Restore the dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Run the tests
        run: npm run test
